const config = {
    db: {
      /* don't expose password or any sensitive info, done only for demo */
      host: "localhost",
      user: "root",
      password: "Mag17615@",
      database: "sql_mysql_node_testing",
    },
    listPerPage: 1000,
  };
  module.exports = config;